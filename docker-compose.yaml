version: '3.8'

services:
  web_panel:
    build: ./web_panel
    ports:
      - "5000:5000"
    volumes:
      - ./web_panel:/app
    environment:
      SECURITY_MODE: SECURE   # SECURE veya VULNERABLE olarak değiştir
    extra_hosts:
      - "host.docker.internal:host-gateway"